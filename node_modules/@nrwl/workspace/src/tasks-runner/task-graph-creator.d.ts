import type { ProjectGraph, TargetDependencyConfig } from '@nrwl/devkit';
import { Task } from './tasks-runner';
export interface TaskGraph {
    roots: string[];
    tasks: Record<string, Task>;
    dependencies: Record<string, string[]>;
}
export declare class TaskGraphCreator {
    private readonly projectGraph;
    private readonly defaultTargetDependencies;
    constructor(projectGraph: ProjectGraph, defaultTargetDependencies: Record<string, TargetDependencyConfig[]>);
    createTaskGraph(tasks: Task[]): TaskGraph;
}
